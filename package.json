{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --https",
    "build": "vite build",
    "serve": "vite build && vite preview"
  },
  "devDependencies": {
    "vite": "^2.7.12"
  },
  "dependencies": {
    "three": "^0.136.0"
  }
}
